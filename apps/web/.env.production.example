# =============================================================================
# Production environment for @scheduling/web (Vercel).
#
# Set these in the Vercel project settings (or `vercel env add ...`). The
# values shown are placeholders — replace with the actual Railway URL of
# your API after the API deploys.
# =============================================================================

# Full https URL of the deployed API. Used by the client + by server
# components for SSR fetches. No trailing slash.
NEXT_PUBLIC_API_URL=https://berberica-api.up.railway.app
